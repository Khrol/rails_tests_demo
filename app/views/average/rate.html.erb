<%= form_for :average, url: { action: "calculate" } do |f| %>
    <p>
      <%= f.label :country %><br>
      <%= f.text_field :country %>
    </p>

    <p>
      <%= f.submit 'Calculate Average' %>
    </p>
<% end %>